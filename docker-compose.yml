services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: marktplatz
      POSTGRES_USER: marktplatz_user
      POSTGRES_PASSWORD: marktplatz_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: